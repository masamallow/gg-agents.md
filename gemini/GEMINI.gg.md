# Global Gemini CLI Instructions

Personal defaults. Project-level `GEMINI.md` may override or extend these.

@~/.codex/AGENTS.md

## Operating principles (Gemini CLI)
- Use `@path` imports to pull in additional context files; avoid pasting large content inline.
- When unsure whether a tool is allowed, ask for confirmation rather than guessing.
